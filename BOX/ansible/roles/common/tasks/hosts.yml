---
#- name: copy /etc/hosts
#  template:
#    src: hosts.j2
#    dest: /etc/hosts
#    backup: yes
#    owner: root
#    group: root
#    mode: 0644

- name: edit /etc/hosts
  blockinfile:
    path: /etc/hosts
    block: "{{ item.ip }} {{ item.name }}"
    marker: "# {mark} ANSIBLE MANAGED BLOCK {{ item.name }}"
  with_items: "{{ hosts }}"